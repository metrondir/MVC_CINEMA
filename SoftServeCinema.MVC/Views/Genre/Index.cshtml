@model IEnumerable<SoftServeCinema.Core.DTOs.Genres.GenreDTO>

@{
    ViewData["Title"] = "Жанри";
    ViewData["PaginatorAction"] = "Index";
}

<div>
    <h3 class="text-center my-4">Жанри</h3>

    @await Html.PartialAsync("_Paginator", Model)
    <div class="list-group my-4">
        @foreach (var genre in Model)
        {
            <a asp-action="Details" asp-route-id="@genre.Id" class="list-group-item list-group-item-action" aria-current="true">
                <div class="d-flex w-100 justify-content-between">
                    <h5 class="mb-1">@genre.Name</h5>
                </div>
            </a>
        }
    </div>
</div>

@await Html.PartialAsync("_Paginator", Model)